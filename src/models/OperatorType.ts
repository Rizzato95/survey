/** Entity status type enum  */
export enum OperatorType {
  Equals = '=',
  LessThan = '<',
  GreaterThan = '>',
  LessEqualThan = '<=',
  GreaterEqualThan = '>=',
  NotEqual = '!=',
  Contains = 'Contains',
  StartsWith = 'StartsWith',
  EndsWith = 'EndsWith',
  In = 'In'
}
