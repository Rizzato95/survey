/**
 * Rappresents a sorting mode
 */
export class SortMode {
    /** The sort mode value, such as ASC | DESC */
    public value!: string;
    /** The sort mode resource key */
    public resourceKey!: string;
}