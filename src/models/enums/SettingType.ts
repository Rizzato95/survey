/**
 * Rappresent a setting type (which fields are valid)
 */
export enum SettingType {
    /** The stringValue is valid */
    String = 'String',
    /** The numericValue is valid */
    Numeric = 'Numeric',
    /** The both values are valid */
    Both = 'Both'
}